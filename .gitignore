# System
.DS_Store
Thumbs.db
nul

# Claude Code (lokale Session-Daten)
.claude/

# PowerShell-Scripts (nur lokal gebraucht)
*.ps1
# API key for KI-Assistent (NICHT committen — wird von OpenRouter automatisch deaktiviert!)
config.js
