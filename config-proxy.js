// Cloudflare Worker Proxy URL (kein API-Key noetig — sicher fuer Public Repos)
// Worker haelt den OpenRouter API-Key serverseitig
window.AMBER_PROXY_URL = "https://amber-proxy.graig-groebli.workers.dev";
