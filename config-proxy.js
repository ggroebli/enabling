// Cloudflare Worker Proxy URL (kein API-Key — sicher fuer Public Repos)
// Worker haelt den OpenRouter API-Key serverseitig
window.AMBER_PROXY_URL = "https://amber-proxy.YOUR_SUBDOMAIN.workers.dev";
